# warning-fix — task1 — worklog

## Avanzamento
- [ ] Verifica invocazione rebuild nativo via npm scripts in package.json
- [ ] Aggiunta blocco config.node_gyp in package.json
- [ ] Rimozione righe msvs_version/clang da .npmrc
- [ ] Verifica rebuild OK + nessun warning npm in dev e build

## Test
(da compilare dopo il test dell'utente: `npm run dev` e `npm run build` senza warning "Unknown project/env config", rebuild nativo completato)
