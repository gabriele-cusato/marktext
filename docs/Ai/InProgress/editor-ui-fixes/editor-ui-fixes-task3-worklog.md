# editor-ui-fixes — task3 — worklog

## Avanzamento
- [ ] Grep usi createQuickInsertObj/renderObj (conferma punti da toccare)
- [ ] fullRenderObj pristino nel costruttore (+ eventuale punto di rigenerazione lingua)
- [ ] search() basata su fullRenderObj
- [ ] Build di verifica

## Test
(da compilare dopo il test utente: @ → ricerca → cancella → lista completa; @ altrove → lista completa; selezione voce inserisce la sintassi giusta)
