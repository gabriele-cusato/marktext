# editor-ui-fixes — task2 — worklog

## Avanzamento
- [ ] Scrittura `file.muyaIndexCursor = liveCursor` nel watch(sourceCode) di editor.vue
- [ ] Verifica ordine flush reattivo scrittura → mount sourceCode
- [ ] Build di verifica

## Test
(da compilare dopo il test utente: cursore a metà documento → switch a source → riga corrispondente; verso source→Muya ancora OK; undo unificato non regredito)
