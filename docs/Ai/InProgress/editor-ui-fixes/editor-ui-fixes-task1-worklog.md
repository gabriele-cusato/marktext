# editor-ui-fixes — task1 — worklog

## Avanzamento
- [ ] try/catch in scrollToCursor (editor.vue) con return silenzioso se nessun cursore attivo
- [ ] Build di verifica

## Test
(da compilare dopo il test utente: rename tab attiva e in background dal context menu — nessun errore selectionChange in console, rename funzionante, scroll-to-cursor normale nei cambi tab)
