# editor-ui-fixes — task4 — worklog

## Avanzamento
- [ ] Scope layout li esteso al sottomenu (`>` rimosso)
- [ ] Span etichetta: flex:1 + min-width 0 + nowrap + ellipsis
- [ ] Verifica altre regole `>` (short-cut) — adattate solo se necessario
- [ ] Build di verifica

## Test
(da compilare dopo il test utente: front menu → style/turn into — voci lunghe su una riga con ellipsis, menu principale invariato, check anche con lingua non-en)
